---
interface Props {
  name: string;
  text: string;
  logo: string;
  url: string;
}
const { name, text, logo, url } = Astro.props;
---

<article class="product-block">
  <div class="product-logo">
    <img src={logo} alt={`${name} Logo`} loading="lazy" />
  </div>
  <div class="product-body">
    <h3>{name}</h3>
    <p>{text}</p>
  </div>
  <div class="product-cta">
    <a href={url} class="btn btn-dark" target="_blank" rel="noopener">
      Berechnen →
    </a>
  </div>
</article>

<style>
  .product-block {
    display: grid;
    grid-template-columns: 200px 1fr auto;
    gap: clamp(1.5rem, 3vw, 3rem);
    align-items: center;
    padding: clamp(1.75rem, 3vw, 2.5rem);
    background: #fff;
    border: 1px solid var(--color-border);
    transition: border-color 300ms ease, box-shadow 400ms var(--ease-out);
  }

  .product-block:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-md);
  }

  @media (max-width: 900px) {
    .product-block {
      grid-template-columns: 160px 1fr;
      grid-template-rows: auto auto;
    }
    .product-cta { grid-column: 1 / -1; justify-self: start; }
  }

  @media (max-width: 560px) {
    .product-block { grid-template-columns: 1fr; padding: 1.5rem; }
    .product-cta { grid-column: 1; }
  }

  .product-logo {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    min-height: 80px;
    padding-right: 1rem;
    border-right: 1px solid var(--color-border);
  }

  @media (max-width: 560px) {
    .product-logo { border-right: 0; padding-right: 0; padding-bottom: 1rem; border-bottom: 1px solid var(--color-border); justify-content: flex-start; }
  }

  .product-logo img {
    max-width: 150px;
    max-height: 80px;
    width: auto;
    height: auto;
    object-fit: contain;
  }

  .product-body h3 {
    margin-bottom: 0.6rem;
    color: var(--color-primary);
    font-size: clamp(1.2rem, 2vw, 1.5rem);
    font-weight: 500;
    letter-spacing: -0.015em;
  }

  .product-body p {
    color: var(--color-text-muted);
    font-size: 0.98rem;
    line-height: 1.55;
    max-width: 54ch;
  }
</style>
